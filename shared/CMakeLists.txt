add_library(repaper STATIC SecretStore.cpp BridgeClient.cpp)
target_include_directories(repaper PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(repaper PUBLIC Qt6::Core Qt6::Network Qt6::Concurrent OpenSSL::Crypto)
if(TARGET Qt6::Quick)
  add_subdirectory(keyboard)
endif()
if(BUILD_TESTING)
  add_executable(repaper-tests tests/test_shared.cpp)
  target_link_libraries(repaper-tests PRIVATE repaper Qt6::Test)
  add_test(NAME repaper-tests COMMAND repaper-tests)
endif()
