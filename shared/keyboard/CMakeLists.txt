add_library(repaper_keyboard STATIC KeyboardController.cpp KeyboardController.h ClipboardBridge.cpp ClipboardBridge.h
    NativeKeyboardClient.cpp NativeKeyboardClient.h)
target_include_directories(repaper_keyboard PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(repaper_keyboard PUBLIC Qt6::Core Qt6::Gui Qt6::Qml Qt6::Quick Qt6::QuickControls2 Qt6::Network)
qt_add_resources(repaper_keyboard repaper_keyboard_qml PREFIX "/repaper/keyboard" BASE qml FILES qml/TouchKeyboard.qml)
if(BUILD_TESTING AND NOT CMAKE_CROSSCOMPILING)
    find_package(Qt6 6.2 REQUIRED COMPONENTS Test)
    add_executable(repaper_keyboard_tests tests/KeyboardTest.cpp)
    target_link_libraries(repaper_keyboard_tests PRIVATE repaper_keyboard Qt6::Test)
    qt_add_resources(repaper_keyboard_tests keyboard_test_qml PREFIX "/keyboard-test" BASE tests FILES tests/KeyboardFixture.qml)
    add_test(NAME repaper_keyboard COMMAND repaper_keyboard_tests)
    set_tests_properties(repaper_keyboard PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen;QT_QUICK_BACKEND=software")
    add_executable(repaper_native_keyboard_tests tests/NativeKeyboardClientTest.cpp)
    target_link_libraries(repaper_native_keyboard_tests PRIVATE repaper_keyboard Qt6::Test)
    qt_add_resources(repaper_native_keyboard_tests native_keyboard_test_qml PREFIX "/native-keyboard-test" BASE tests FILES tests/KeyboardFixture.qml)
    add_test(NAME repaper_native_keyboard COMMAND repaper_native_keyboard_tests)
    set_tests_properties(repaper_native_keyboard PROPERTIES ENVIRONMENT "QT_QPA_PLATFORM=offscreen;QT_QUICK_BACKEND=software")
endif()
